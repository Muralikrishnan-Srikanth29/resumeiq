[tool.ruff]
line-length = 110
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = ["E501"]  # line length handled by line-length above, not a hard error

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
